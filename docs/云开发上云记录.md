# 云开发上云记录（2026-08-22）

> 用途：记录本次把数据 + 云函数部署上云的完整过程和踩过的坑，方便团队查看与后续继续。
> 对应清单见 `docs/云开发上云清单.md`；真实密钥只存 `.env` 与 `docs/ask环境变量.md`（已 gitignore），本文档不含密钥。
> 执行人：P3（shizelengleng）｜ 由 AI 助手按实际操作过程整理

---

## 一、已完成步骤

| # | 步骤 | 结果 |
|---|---|---|
| 1 | 微信开发者工具导入项目（根目录 `xcx`，AppID `wxcbe9b5d978378d3b`） | ✅ |
| 2 | 开通云开发环境 | ✅ |
| 3 | 建 3 个集合：`documents` / `policies` / `conversations` | ✅ 权限已设：前两个「所有用户可读，仅创建者可读写」，`conversations`「仅创建者可读写」 |
| 4 | 导入数据 | ✅ `documents` 37 条、`policies` 37 条 |
| 5 | 部署 4 个云函数（`ask` / `getPolicies` / `saveHistory` / `getHistory`，均用「云端安装依赖」） | ✅ |
| 6 | 给 `ask` 配置 12 个环境变量 | ⏳ **进行中，遇坑**（见下文） |
| 7 | 重新部署 `ask` 并测试第一条问答 | ⏳ 待做 |

---

## 二、踩过的坑（重要，下次别踩）

### 坑 1：数据库导入报「格式不正确，请检查是否为 JSON Lines 格式」

- **现象**：导入 `data/documents.json`（JSON 数组格式）报错。
- **原因**：微信云开发数据库导入要求文件内容为 **JSON Lines**（每行一条记录），不是 JSON 数组（一个 `[` 包全部）。
- **关键细节**：文件**后缀必须是 `.json`**（`.jsonl` 后缀它不认），但**内容**是每行一条。
- **解决**：把数组转成 JSONL 内容，文件名仍用 `.json`：
  - `data/documents_import.json`（37 行）
  - `data/policies_import.json`（37 行）
- **规律**：以后向云数据库灌数据，一律「`.json` 后缀 + 每行一条 JSON」。

### 坑 2：ask 环境变量拼 URL 报 `Invalid URL`

- **现象**：测试时返回 `ask 云函数异常: TypeError [ERR_INVALID_URL]: Invalid URL`，报错里 URL 为 `DASHSCOPE_API_KEY=sk-ws-.../embeddings`。
- **原因**：`ask` 用 `EMBEDDING_BASE_URL/embeddings` 拼地址（`ask/index.js` 57 行）。报错说明 **`EMBEDDING_BASE_URL` 这项被填成了 `DASHSCOPE_API_KEY=sk-ws-...` 这整段**——配置环境变量时把 `名=值` 一行糊进了某一栏。
- **解决**：MD 文件里每行 `名字=值` 要拆成两栏——左边填 `=` 前的名字，右边只填 `=` 后的值。例：`EMBEDDING_BASE_URL` 的值必须是纯网址 `https://ws-.../compatible-mode/v1`，`DASHSCOPE_API_KEY` 的值必须只有 `sk-ws-...` 本身，都不带 `名字=` 前缀。

### 坑 3（安全，已处理）：真实密钥曾误入公开仓库

- **现象**：写 `云开发上云清单.md` 时把真实 API Key 直接填进文档并提交（commit `9b2cf8e`）。
- **处理**：
  1. 已把该文档里的真实 key 全部换成「见本地 `.env`」占位（commit `e136488`）；
  2. 新增 `docs/ask环境变量.md`（含真实 key，仅本地用）并加入 `.gitignore`；
  3. 教训：**任何含真实 key 的文件不得进公开仓库**，密钥只在 `.env` / `ask环境变量.md` 本地。
- **残留风险**：commit `9b2cf8e` 在 git 历史里仍有旧 key（公开仓库历史删不掉）。决定**先用现有 key，8/31 赛后轮换**（百炼 / 智谱 / SenseNova 三个平台重新生成）。

---

## 三、待完成

1. 修正 ask 的 12 个环境变量（重点检查 `EMBEDDING_BASE_URL` 和 `DASHSCOPE_API_KEY` 是否填对）。
2. 重新部署 `ask`（右键 → 上传并部署：云端安装依赖）让环境变量生效。
3. 测试：ask 云函数「云端测试」输入 `{"question":"港澳青年子女怎么入读天河区小学？"}`，确认返回 `answer` + `sources`（出处）。
4. 通过后：前端 `miniprogram/utils/api.js` 里把注释掉的 `wx.cloud.callFunction` 替换 mock（P2 负责）。

---

## 四、当前线上配置速查

| 项 | 值 |
|---|---|
| 集合 | `documents`（37）/ `policies`（37）/ `conversations`（0） |
| 云函数 | `ask`、`getPolicies`、`saveHistory`、`getHistory` 已部署 |
| ask 环境变量 | 12 个（`EMBEDDING_BASE_URL` / `DASHSCOPE_API_KEY` / `ZHIPU_API_KEY` / `SENSENOVA_KEYS` 等），值见本地 `.env` |
| 检索 | 向量模型 `text-embedding-v4`，1024 维，Top4 |
| 生成 | 主力 `glm-4.7-flash` → 备选 `glm-4-flash` → 兜底 SenseNova |
