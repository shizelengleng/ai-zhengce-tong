# 项目现状存档（AI政策通）

> 用途：团队上下文备份，任何人接手都能快速了解项目全貌。
> 最近更新：2026-08-22（P3 后端上云 + 全链路打通 + 37 篇全文重建 + 联网搜索兜底）｜ 由 AI 助手整理 ｜ 真实密钥不写入本文档（见 `.env`）

---

## 1. 项目一句话

**「AI政策通」**：天河区居民用日常口语向微信小程序提问，AI 基于天河区真实政策知识库回答，并附官方出处与办事入口。

参赛项目：**2026 年青AI天河人工智能协作创新大赛**（板块二·AI应用创新·方向1 社区便民）。

---

## 2. 赛事硬性要求

| 项 | 内容 |
|---|---|
| 截止 | **2026-08-31 24:00**（逾期不受理） |
| 提交邮箱 | TianQingA/2026@126.com |
| 需提交 | ① 作品原件（代码包 + 演示视频 2-3 分钟）② 作品说明（创作思路 / AI技术亮点 / 与天河关联性 / 落地价值）③ 原创承诺书（签字扫描件） |
| 压缩包命名 | `板块名称+姓名/团队名称+作品名称` |
| 奖项参考 | 板块二：一等 10000 / 二等 5000 / 三等 2000；最佳落地应用奖 2000 |

---

## 3. 产品

- **定位**：把天河区海量政策文件，变成"人人能问、一问就懂、带办事入口"的政策百晓生。
- **目标用户**：港澳青年家长、来穗新市民、创业者、老年居民子女。
- **核心功能（MVP）**：对话问答 / 政策分类浏览（**8 类**）/ 政策详情 / 问答历史。
- **页面**：`chat`（聊天）· `category`（分类）· `detail`（详情）· `mine`（我的）。
- **差异化亮点**：答案**可溯源**（附官方出处）、数据全部为**天河真实政策**、内置**演示脚本**。

---

## 4. 技术方案（已确认）

```
用户提问 → 云函数ask → 问题转向量 → 向量检索Top4
        → 按doc_ids拉官方原文 → 拼Prompt（只依据原文） → 大模型生成 → 答案+出处
        → 原文缺关键信息时：联网搜索补官方资料 → "联网补充"小节
```

- **前端**：微信原生小程序（微信开发者工具），不引第三方 UI 库。
- **后端**：微信云开发（云函数 + 云数据库 + 云存储），免服务器、免域名备案、外呼 API 不受域名白名单限制。云环境 ID：`cloud1-d8g5hsfuke3b9e36e`。
- **RAG 检索**：阿里云百炼 `text-embedding-v4`（1024 维，OpenAI 兼容接口）。知识库几十条，云函数内存算余弦相似度即可，**不需要向量数据库**。
- **回答生成**：大模型主备切换 + 熔断。云上实际配置：**主力 SenseNova 网关**（`glm-5.2` → `deepseek-v4-flash`，5 key 随机）；**兜底智谱**（`glm-4.7-flash` → `glm-4-flash`）；连续失败 2 次熔断 5 分钟自动恢复，`PRIMARY_PROVIDER` 可切换。`MAX_TOKENS=4096`（2048 曾导致长回答截断）。
- **云函数清单**：`ask`（向量检索+拉原文+Prompt+调模型）、`getPolicies`（分类/详情，返回 `{policies}`）、`saveHistory`、`getHistory`。
- **数据库集合**：`documents`（官方原文，**37 篇已全文重建**，`original_text` ≥200 字，多篇 4000-8000 字）、`policies`（知识条目，37 条，含 `doc_ids`、`vector` 字段）、`conversations`（问答历史）。
- **Prompt 原则**：只依据"官方原文"回答、不得编造、给出处（标题+文号+链接）、多政策冲突以广州/天河本地为准、无匹配先联网再引导 12345。
- **接口约定**（文档 §5.2）：`ask→{answer,sources}`、`getPolicies→{policies}`、`saveHistory→{success}`、`getHistory→{history}`（云函数与前端已对齐）。

---

## 5. 账号与 AI 资源（状态总览）

> 所有真实密钥存放于本地 `.env` 与 `docs/ask环境变量.md`（已加入 `.gitignore`，绝不提交）。

| 资源 | 状态 |
|---|---|
| 微信云开发 | ✅ 已开通，环境 `cloud1-d8g5hsfuke3b9e36e`；`documents`/`policies` 各 37 条，`conversations` 0 |
| 阿里云百炼（embedding） | ✅ 已注册；接口实测可调用；WorkspaceId `ws-05xef78zs5uw6cxy`；免费 100 万 token / 90 天 |
| SenseNova 网关（生成·主力） | ✅ 5 个 key 已存入 `.env`；主用 `glm-5.2`，失败换 `deepseek-v4-flash` |
| 智谱 GLM（生成·兜底） | ✅ 已注册；`glm-4.7-flash` → `glm-4-flash`；连续失败 2 次熔断 5 分钟自动恢复 |
| 博查 AI Search（联网兜底） | ✅ 已开通 `BOCHA_API_KEY`；原文缺材料/电话/流程时自动联网补官方资料 |
| Uniapi | 备用，已存入 `.env`，未实测 |
| 微信小程序 AppID | ✅ `wxcbe9b5d978378d3b`（P2 reed9988 已注册并建好项目） |

---

## 6. 仓库与文件

- **GitHub 仓库**：https://github.com/shizelengleng/ai-zhengce-tong （**公开**，分支 main，远程已切 HTTPS）
- 本地根目录：`C:\Users\123\Desktop\xcx`

```
xcx/
├── .env                    本地机密配置（真实key，绝不提交）
├── .env.example            配置模板（队友参照）
├── .gitignore              （含 docs/ask环境变量.md 忽略规则）
├── README.md
├── project.config.json     AppID + miniprogramRoot + cloudfunctionRoot 已配
├── docs/
│   ├── AI政策通-项目流程说明.md   （v1.1，含真RAG升级）
│   ├── 团队分工与开发路径.md
│   ├── 项目现状存档.md            ← 本文档
│   ├── 云开发上云清单.md          ← 部署步骤（已脱敏，不含key）
│   ├── 云开发上云记录.md          ← 上云过程 + 5个坑 + 验证结果
│   ├── 后续优化路线图.md          ← 优化方向（时效性/质量/数据库/追问）
│   ├── ask环境变量.md            ⚠️ 真实key，本地用，已gitignore
│   └── policy_sources/          官方原文存档（46个文件）
├── data/
│   ├── 知识条目表.csv            37 条知识条目（9列已填）
│   ├── 官方文件表.csv            37 篇官方文件（9列已填）
│   ├── documents.json           官方原文（csv_to_json.py 生成）
│   ├── policies.json            知识条目含 doc_ids（csv_to_json.py 生成）
│   ├── policies_with_vector.json 向量化结果（37条，1024维）
│   ├── documents_import.json    云数据库导入文件（JSONL内容+.json后缀）
│   └── policies_import.json     云数据库导入文件（同上）
├── scripts/               CSV→JSON + 向量化 + 原文重建脚本（csv_to_json.py / embed_policies.py / _fill_batch.py / _rebuild_all_docs.py）
├── miniprogram/           小程序前端（P2 建好，已接真后端）
└── cloudfunctions/        4个云函数（ask/getPolicies/saveHistory/getHistory，已部署）
```

---

## 7. 团队分工速览

| 角色 | 认领人 | 职责 | 负责的数据分类 | 依赖 |
|---|---|---|---|---|
| P1 队长·统筹+数据 | ✅ **kdbbm9t62c-alt** | 排期、赛事对接、政策数据（牵头）、演示视频、作品说明、提交 | 政务办事 / 社保补贴 | 全局 |
| P2 前端·产品 | ✅ **reed9988**（登录名 13360389988） | 4 个页面 + 交互 + 真机预览 | 教育入学 / 医疗保障 | 依赖 P3 云函数接口 |
| P3 后端·AI | ✅ **shizelengleng（用户本人）** | 云开发、云函数、向量化、AI 接入 | 住房 / 就业创业 / 港澳青年 | 依赖 P1 数据与 AppID |

> 三人均**基本不会代码**，代码由 AI 生成，各自"照步骤粘贴 + 调通 + 填数据 + 测试"。
> ✅ AppID（`wxcbe9b5d978378d3b`）已由 P2 注册完成。完整细节见 `docs/团队分工与开发路径.md`。

---

## 8. 开发排期红线

| 日期 | 红线 | 状态 |
|---|---|---|
| 8/20 | 三人账号、工具、仓库全部就绪 | ✅ |
| 8/22 | 首批 30 条政策数据入库 | ✅ 37 条 |
| 8/24 | `ask` 云函数能回答第一条政策 | ✅ **8/22 提前跑通** |
| 8/26 | 4 个页面全部完成 | ✅ **前端已接真后端** |
| 8/28 | 真机可完整问答，链路稳定 | ⏳ |
| 8/29 | 演示视频录好 | ⏳ |
| 8/30 | 作品说明 + 承诺书 + 打包 | ⏳ |
| 8/31 | 提交邮箱并确认收到 | ⏳ |

---

## 9. 已完成事项

1. 项目流程说明文档 v1.1（含向量检索真 RAG 升级）
2. 团队分工与开发路径文档
3. GitHub 公开仓库 `ai-zhengce-tong` 建立并推送
4. 两张 CSV：政策数据初稿 + 项目进度表模板
5. 全部 AI Key 实测验证，`.env` 与 `.env.example` 配置完成
6. 阿里云 embedding 接口实测通过（model `text-embedding-v4`，dim 1024）
7. 两名队友协作者邀请已发出（Write 权限）
8. **P2 前端已合并**：4 页面 + `project.config.json` + mock 层（提交 `d8f2857`/`8fec1d8`）
9. **云函数返回格式已对齐**文档 §5.2 与前端（`getPolicies`→`{policies}`、`getHistory`→`{history}`、`saveHistory`→`{success}`）
10. **政策数据 37 条**已向量化（`policies_with_vector.json`，1024 维）
11. **前端补足 8 分类**（原 6 类，补 welfare/elderly）
12. **云开发上云完成**：3 集合 + 37/37 数据导入 + 4 云函数部署 + ask 环境变量 13 个（含 BOCHA_API_KEY）+ 超时 60s
13. **ask 端到端跑通**：实测"港澳青年子女入学"返回完整答案 + 4 条官方出处（用调试器 Console 调用，网页控制台测试通道有坑见记录）
14. **前端接真后端**：app.js 初始化云开发 + api.js 换真实 `wx.cloud.callFunction`，删除废弃 mock.js（提交 `c087ef8`/`766f79c`）
15. **文档体系建立**：上云清单 / 上云记录（5 个坑）/ 后续优化路线图
16. **ask 回答质量优化**：5 段标准（谁能办/要什么条件/怎么办/交什么材料/注意）+ 相似度阈值兜底（`MIN_SCORE` 0.4，无匹配引导 12345）+ 出处按标题去重 + 多政策冲突以广州/天河本地为准（提交 `4d10e91`）
17. **37 篇官方原文全文重建**：`scripts/_rebuild_all_docs.py` 从 46 个本地官方 HTML 提取正文（锚点定位+导航去噪+页脚截断，单篇上限 8000 字）；doc_5 招生细则 161→2176 字、doc_32 居家养老 342→8000 字（提交 `e8fc8c0`）
18. **模型主备切换 + 熔断**：SenseNova 主力（glm-5.2 → deepseek-v4-flash，5 key 随机）→ 智谱兜底（glm-4.7-flash → glm-4-flash）；`MAX_TOKENS` 2048→4096 修复长回答截断
19. **联网搜索兜底（博查 AI Search）**：原文缺材料/电话/流程时自动联网补官方资料（`【信息不足】`标记 → 定向检索 → "联网补充"小节）；出处区分「网络检索·官网」与「网络检索（非官方，仅供参考）」；本地库无匹配先联网再 12345（提交 `87e3199`）
20. **优化路线图 v0.3**：全链路已通 + 已完成项 ✅ 标记 + 按"演示不翻车 > 数据更全 > 加分项"重新排期（8/31 红线不变）

---

## 10. 待对接 / 未决事项（按优先级）

| 优先级 | 事项 | 负责人 |
|---|---|---|
| 🔴 | **赛后轮换 API Key**（因曾误提交进公开仓库历史 commit `9b2cf8e`，当前文件已清理，历史残留） | P3 主导 |
| 🟡 | **数据扩充 37 → 100+ 条**（每分类 10-15 条） | 三人分头（P1政务社保/P2教育医疗/P3住房就业港澳） |
| 🟡 | **政策时效性**：数据加 `status`/`effective_date`，ask 检索过滤失效政策 | P3 |
| 🟡 | **回答质量**：5 段/阈值/去重/本地优先 已上线 ✅；剩 `doc_no` 文号补全 + `source_url` 逐一校验 | P3 |
| 🟢 | **预设追问 / 多轮对话**（演示加分项） | P3 后端 + P2 前端 |
| 🟢 | 真机预览 + 录演示视频 | P2 + P1 |
| 🟢 | 作品说明 + 原创承诺书 + 打包 | P1 |

---

## 11. 下一步行动（8/22 起）

1. ✅ 云开发上云 + ask 跑通 + 前端接真后端 + 37 篇全文重建 + 联网搜索兜底（已完成）。
2. **提交物（最优先，8/31 红线）**：演示视频 + 作品说明 + 原创承诺书 + 打包，P1 牵头，P3 出问答脚本。
3. 测试问题集 10-20 条 + 打分（兼作演示视频问答脚本，P3 出初稿）。
4. 真机完整问答 + 延迟确认：P2 真机跑通完整链路。
5. 数据扩充 37 → 100+：三人分头填 CSV，P3 重跑 `_rebuild_all_docs.py` / `embed_policies.py` 后重新入库 + 向量化。
6. 政策时效：数据加 `status`/`effective_date`，ask 检索前过滤失效政策（正确性底线）。
7. 加分项：预设追问（`followups`）+ 多轮对话（`history`），时间够再做。

---

## 12. 安全提醒（重要）

- `.env` 含真实 API Key，**绝不提交、不外传**；`docs/ask环境变量.md` 已 gitignore。
- 仓库为**公开**：任何文档不得写真实 key（已发生过一次泄露：commit `9b2cf8e`，当前已清理）。
- **2026-08-31 赛后务必轮换**全部 API Key（百炼 / 智谱 / SenseNova）。
- 云函数一律从环境变量读 key，代码里绝不硬编码。
